local sendtoterm = require("sendtoterm")

describe("setup", function()
  -- it("works with default", function()
  --   assert(sendtoterm.hello() == "Hello!", "my first function with param = Hello!")
  -- end)

  -- it("works with custom var", function()
  --   sendtoterm.setup({ opt = "custom" })
  --   assert(sendtoterm.hello() == "custom", "my first function with param = custom")
  -- end)
end)
